export { default } from './TweetBody'

/* index.ts exists for the sake of cleaner import trails. Rather than any confusion around TweetBody being imported or having a catch-all Tweet function host most of the code, we simply export the finished Tweet from TweetBody. */
